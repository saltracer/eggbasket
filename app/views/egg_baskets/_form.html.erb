<%= form_for(@egg_basket) do |f| %>
  <% if @egg_basket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@egg_basket.errors.count, "error") %> prohibited this egg_basket from being saved:</h2>

      <ul>
      <% @egg_basket.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :egg_count %><br>
    <%= f.text_field :egg_count %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
