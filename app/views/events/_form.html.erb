<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :occurred_on %><br>
    <%= f.date_select :occurred_on %>
  </div>
  <div class="field">
    <%= f.label :egg_basket_id %><br>
    <%= f.text_field :egg_basket_id %>
  </div>
  <div class="field">
    <%= f.label :cost %><br>
    <%= f.text_field :cost %>
  </div>
  <div class="field">
    <%= f.label :event_name_id %><br>
    <%= f.text_field :event_name_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
